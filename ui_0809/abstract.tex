%Example blueprints for a technique paper
%<Users> trying to perform <task> fail, because the <bestKnownSolution> has <limitation>. In this paper, we present <proposedSolution>. It overcomes <limitation> by <newFeatureOrApproach>. In a user study, participants completed <task> <percentage> faster/more accurate when using <proposedSolution> than when using <bestKnownSolution>.
